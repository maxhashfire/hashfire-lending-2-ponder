# Ponder RPC URL for Avalanche C-Chain
# Use a private RPC for better rate limits in production
PONDER_RPC_URL_43114=https://avalanche-mainnet.infura.io/v3/ee9ace694999466db35636ceac1d39eb

# RPC rate limiting (adjust based on your RPC provider)
MAX_RPC_REQUESTS_PER_SECOND=50
POLLING_INTERVAL_MS=4000

# Optional: Database URL for production (Ponder uses SQLite by default in dev)
# DATABASE_URL=postgres://user:password@host:5432/hashfire_lending_2
